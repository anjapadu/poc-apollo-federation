"Enumeration of supported file types for exporting reports"
enum FileOutputTypeEnum {
  CSV
  XLSX
  PDF
}

enum FileOutputEnum {
  DEVICE_ACTIVITY_REPORT
  DEVICE_ACTIVITY_PER_DEVICE_REPORT
  DEVICE_EVENTS_REPORT
  INSTALLED_DEVICES_REPORT
  PENDING_INSTALLATION_REPORT
  REGISTRATIONS_REPORT
  UNINSTALLED_HISTORY_REPORT
}
